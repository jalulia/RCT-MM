// Export the same small scenes used beside the reader text.
const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),os=require('node:os'),crypto=require('node:crypto'),{spawnSync}=require('node:child_process');
const {createCanvas}=require('@napi-rs/canvas'),root=path.resolve(__dirname,'..'),out=path.join(root,'previews/reading'),temp=fs.mkdtempSync(path.join(os.tmpdir(),'mmt-reading-')),v=vm.createContext({window:{}}),sources=['pixel-kit.js','park-kit.js','reading-moments.js'];
fs.mkdirSync(out,{recursive:true});for(const f of sources)vm.runInContext(fs.readFileSync(path.join(__dirname,f),'utf8'),v);
try{
 for(const kind of ['workshop','handoff'])for(let i=0;i<80;i++){const c=createCanvas(320,184),ctx=c.getContext('2d');v.window.MMTArt.readingScene(c,i/10,kind);ctx.globalCompositeOperation='destination-over';ctx.fillStyle='#ffffff';ctx.fillRect(0,0,320,184);fs.writeFileSync(path.join(temp,kind+'-'+String(i).padStart(2,'0')+'.png'),c.toBuffer('image/png'));}
 const python=`from PIL import Image\nfrom pathlib import Path\nimport sys,json\nsrc,out=map(Path,sys.argv[1:]);result=[]\nfor kind in ['workshop','handoff']:\n frames=[Image.open(p).convert('RGB') for p in sorted(src.glob(kind+'-*.png'))]\n frames[0].save(out/(kind+'.png'))\n swatch=Image.new('RGB',(320,184*8))\n for i,im in enumerate(frames[::10]):swatch.paste(im,(0,i*184))\n palette=swatch.quantize(colors=256)\n quantized=[im.quantize(palette=palette,dither=Image.Dither.NONE) for im in frames]\n quantized[0].save(out/(kind+'.gif'),save_all=True,append_images=quantized[1:],duration=100,loop=0,disposal=1)\n with Image.open(out/(kind+'.gif')) as gif:\n  duration=0\n  for i in range(gif.n_frames):gif.seek(i);gif.load();duration+=gif.info.get('duration',0)\n  assert duration==8000 and gif.size==(320,184) and gif.info['loop']==0\n  result.append({'id':kind,'durationMs':duration,'frames':gif.n_frames,'size':list(gif.size)})\nprint(json.dumps(result))`;
 const run=spawnSync(process.env.MMT_PYTHON||'python3',['-c',python,temp,out],{encoding:'utf8'});if(run.status)throw Error(run.stderr);const entries=JSON.parse(run.stdout);fs.writeFileSync(path.join(out,'manifest.json'),JSON.stringify({entries,sources:sources.map(f=>({file:'../../working/'+f,sha256:crypto.createHash('sha256').update(fs.readFileSync(path.join(__dirname,f))).digest('hex')}))},null,2)+'\n');console.log(run.stdout.trim());
}finally{fs.rmSync(temp,{recursive:true,force:true});}
